name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
{% raw %}
        repo-token: ${{ secrets.GITHUB_TOKEN }}
{% endraw %}
        issue-message: 'Greetings to {{cookiecutter.repo_name}} and thanks for opening this issue'
        pr-message: 'Thanks for this contribution to {{cookiecutter.repo_name}} 🥳'

